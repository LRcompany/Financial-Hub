# Frontend

Ainda não iniciado — por decisão do Luiz, a ordem de construção é estrutura → backend/mecânica → UI por último (ele quer discutir UX/UI com calma antes de codar, já trouxe uma referência visual pra essa conversa).

Quando começar: React + Vite, como PWA (manifest + service worker), consumindo a API do `../backend`.
